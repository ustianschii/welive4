export interface TransparentCardProps {
  data: string[][];
  index: number;
  titlestart?: string;
  titleend?: string;
  titlehighlightedstart?: string;
  titlehighlightedend?: string;
  bg: string;
}
